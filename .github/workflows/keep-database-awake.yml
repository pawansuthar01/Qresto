name: Keep Database Awake

on:
  schedule:
    - cron: "*/30 * * * *"
  workflow_dispatch:

jobs:
  ping-health-api:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Ping health endpoint
        env:
          HEALTHCHECK_URL: ${{ secrets.HEALTHCHECK_URL || vars.HEALTHCHECK_URL }}
        run: |
          if [ -z "$HEALTHCHECK_URL" ]; then
            echo "::warning::HEALTHCHECK_URL is not set. Skipping health ping. Set it as a repository secret or variable (for example: https://your-domain.com/api/health)."
            exit 0
          fi

          curl --fail --show-error --silent --max-time 20 "$HEALTHCHECK_URL"
